# Jail Plugin

This folder stores the `JailPlugin` plugin versions.

- `versions/1.21.11/` — plugin source compatible with Minecraft 1.21.11.
- `development/` — optional active working copy.

Versioning policy: keep each released Minecraft-compatible build in its own folder under `versions/` so bug fixes can be applied per-version.
